const Input = ({ input }) => {
  return (
    <div>
      <div className="  px-4 text-right text-white text-[20px] font-semibold">
        {input}
      </div>
    </div>
  );
};

export default Input;
